</br><p id="notice"><%= notice %></p>

<h1>All reviews</h1>
<div class="row">
  <table class="hover">
    <thead>
      <tr>
        <th style="width: 25%"><%= sortable "name", "Artist"  %></th>
        <th style="width: 25%"><%= sortable "title", "Album"  %></th>
        <th style="width: 25%"><%= sortable "rating", "Review" %></th>
        <th style="width: 8%" class="text-center"><%= sortable "rating" %></th>
        <th style="width: 25%"><%= sortable "displayname", "User"  %></th>
      </tr>
    </thead>
      <tbody>
      <% @reviews.each do |review| %>
        <tr>
          <td style="width: 25%"><%= link_to review.album.artist.name, review.album.artist%></td>
          <td style="width: 25%"><%= link_to  review.album.title, review.album%></td>
          <td style="width: 25%"><%= link_to  review.contents%></td>
          <td style="width: 8%" class="text-center"><%= review.rating%></td>
          <td style="width: 25%"><%= link_to  review.user.displayname, review.user%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<div class="row">
  <div class="large-12 columns">
    <%= will_paginate @reviews, previous_label: h("<"),next_label: h(">") %><br>
  </div>
</div>
